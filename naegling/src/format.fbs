namespace Pcodec.Naegling;

table NaeChunkChannel {
  quantization: uint32;
  weights: [int16];
  bias: int32;
  pco_meta: [ubyte];
  pco_page: [ubyte];
}

table Nae {
  h: uint32;
  w: uint32;
  c: uint32;
  chunk_h: uint32;
  chunk_w: uint32;
  pco_header: [ubyte];
  chunks: [NaeChunkChannel];
}

root_type Nae;